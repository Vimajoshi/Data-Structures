#include <bits/stdc++.h>
using namespace std;

int main()
{
    //int arr[10];
    // the name of the array denotes the address of the first memory block of the array
    // cout<<"Address of the first block of the array(Arr[0]): "<<arr<<endl;
    // int *ptr = &arr[0];
    // cout<<"Another method: "<<ptr;
    // cout << endl;
    // ptr++;
    // cout<<"Arr[1]: "<<ptr<< endl;
    // int *ptr2 = &arr[1];
    // cout<<"2nd method arr[1]: "<<ptr2;
    // cout << endl;
    // arr[i] = *(arr+i)

    int arr[] = {1,2,3,4};
    cout<<"arr(int): "<<arr<<endl;

    char ch[] = "helmo";
    cout<<"ch(character): "<<ch<<endl;

    // the implementation of cout is diffrent for int and char array
    
    return EXIT_SUCCESS;
}